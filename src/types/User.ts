//Se crea el objetos user como interface y se exporta
export interface User {
    id:number,
    name:string,
    password:string,
}

export type User2 = {
    id:number,
    name:string,
    password:string,
}